# calculator — challenge starter

A minimal, buildable SvelteKit skeleton. **Read [PROJECT-BRIEF.md](PROJECT-BRIEF.md) first** — it defines the features, the 4 work lanes, the collision map, and the milestones. Run rules + the verbatim startup instruction: ../CHALLENGE.md

```bash
npm install
npm run dev      # develop
npm run build    # the acceptance gate
```
